# Server Configuration
server.address=0.0.0.0
server.port = 8087
spring.application.name = PayrollResourceSoftware

spring.security.user.name=admin
spring.security.user.password=$2a$12$47MgXxcAT1H9Y.raZssegevhhOJMtn.VgneBwzn5hTTJg1Js4OwbW

#logging.level.org.springframework=DEBUG

# DataSource Configuration
spring.datasource.url=jdbc:sqlserver://localhost:1433;database=hrisof;user=admin;password=sa;trustServerCertificate=true;encrypt=true;
spring.datasource.driverClassName=com.microsoft.sqlserver.jdbc.SQLServerDriver
spring.datasource.username=admin
spring.datasource.password=sa
spring.jpa.database-platform=org.hibernate.dialect.SQLServer2012Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# ── Batch Payroll Computation ─────────────────────────────────────────────────
# URLs of other HRIS microservices (bootstrap defaults — overridden at startup from SystemConfig)
hris.services.administrative.url=http://localhost:8082
hris.services.timekeeping.url=http://localhost:8083
hris.services.hrmanagement.url=http://localhost:8085

# Number of parallel worker threads for employee computation.
# Set to 0 (default) to auto-size at (2 x CPU cores), capped at 64.
hris.payroll.batch.compute-threads=0

# The following payroll constants have been moved to the Administrative module
# (PremiumMultiplier and GSISContribution tables) and are no longer read from here.
# They are fetched dynamically by PayrollBatchServiceImpl before each batch run.
# payroll.cutoff.days=22                ← now in PremiumMultiplier.cutoffDays
# payroll.gsis.ps-rate=0.09             ← now in GSISContribution.employeeSharePercentage
# payroll.gsis.er-rate=0.12             ← now in GSISContribution.employerSharePercentage
# payroll.pagibig.mandatory-amount=100.0 ← now in PremiumMultiplier.pagibigMandatoryAmount
# payroll.earned-leave-per-period=1.25  ← now in PremiumMultiplier.earnedLeavePerPeriod